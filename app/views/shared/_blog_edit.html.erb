<%= link_to fa_icon('pencil-square-o'), edit_blog_path(:locale, blog) %>
<%= link_to fa_icon('trash'), blog_path(:locale, blog), method: :delete, data: { confirm: "Are you sure?" } %>
<%= link_to fa_icon('file-text'), toggle_status_blog_path(:locale, blog), class: blog_status_color(blog) %>
