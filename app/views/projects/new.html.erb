<div class="container portfolio-container">
  <h1><%= t('.title') %></h1>
  <%= form_for(@project, url: {action: "create", locale: I18n.locale}) do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
