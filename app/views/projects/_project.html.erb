<div class="col-md-4">
  <div class="card" data-id="<%=project.id%>">
    <%=image_tag project_img(project.thumb_image, 'thumb') , width: '100%' %>
    <p class="card-text">
      <span> <%=link_to project.title, portfolio_show_path(project) %></span> <%=project.subtitle %>
    </p>
  </div>
</div>
